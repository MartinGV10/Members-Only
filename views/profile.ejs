<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title><%= title %></title>
</head>
<body>
    <%- include('nav', {user: user}) %>
    <div class="form-cont">
        <form action="/profile/<%= user.username %>" method="post">
            <h1 class="title"><%= title %></h1>
            <div class="f-bit">
                <div class="f-item">
                    <label for="firstName">First Name</label>
                    <input type="text" name="first_name" id="first_name" value="<%= user.first_name %>">
                </div>
    
                <div class="f-item">
                    <label for="lastName">Last Name</label>
                    <input type="text" name="last_name" id="last_name" value="<%= user.last_name %>">
                </div>
            </div>

            <div class="f-bit">
                <div class="f-item">
                    <label for="username">Username</label>
                    <input type="text" name="username" id="username" value="<%= user.username %>">
                </div>
                
                <div class="f-item">
                    <label for="member">Become a member</label>
                    <input type="password" name="is_member" id="is_member">
                </div>
                
                <div class="f-item ">
                    <label for="admin">Become an admin</label>
                    <input type="password" name="is_admin" id="password">
                </div>
            </div>

            <button type="submit">Submit</button>
        </form>
    </div>
    
</body>
</html>